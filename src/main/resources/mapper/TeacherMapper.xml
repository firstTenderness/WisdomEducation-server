<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dream.mapper.TeacherMapper">

    <select id="list" parameterType="com.dream.pojo.EmpQueryParam" resultType="com.dream.pojo.Teacher">
        SELECT * FROM person WHERE role_type = 2
        <if test="name != null">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="gender != null">
            AND gender = #{gender}
        </if>
        <if test="begin != null">
            AND create_time >= #{begin}
        </if>
        <if test="end != null"> AND create_time &lt;= #{end}</if>
        <if test="degree != null">
            AND degree = #{degree}
        </if>
        <if test="clazzName != null">
            AND clazz_name = #{clazzName}
        </if>
        ORDER BY create_time DESC
        <if test="page != null and pageSize != null">
            LIMIT #{page}, #{pageSize}
        </if>
        <if test="page == null or pageSize == null">
            LIMIT 0, 10
        </if>
    </select>
</mapper>